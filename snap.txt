1.PAGE_SIZE
<Box sx={{ height: '400px', width:'100%', mt:2}}>
                { (totalPages !== 0) && <Stack spacing={2}>
                  <Pagination count={totalPages} showFirstButton showLastButton onChange={(event: React.ChangeEvent<unknown>, value: number) => {setFilterCriteria({...filterCriteria, pageNo:value})}}/>
                </Stack> }
                <Root><DataGrid
                rowHeight={25}
                disableColumnMenu
                getRowId={r => r.categoryId}  
                hideFooterPagination  
                pageSize={20}
                rows={alertConfigList}
                columns={alertConfigTableColumns}
                sortingMode='server'
                sortModel={sortModel}
                sortingOrder={['asc','desc']}
                onSortModelChange={(newModel: GridSortModel) => setSortModel(newModel)}
                loading={false} 
                hideFooterSelectedRowCount
            />
            </Root>
          </Box>



2.PARAMS VALUE
<div>
          {(menuRights?.includes(MenuRights.VIEW) || menuRights?.includes(MenuRights.EDIT))
            ? (<Link to={{ pathname: `${RoutePathConstant.BATCH}`, state: { jobName: params.value } }}>{params.value}</Link>)
            : (<div>{params.value}</div>)}
        </div>

3.RENDERS_INPUT
<LocalizationProvider dateAdapter={AdapterDateFns}>
                  <DatePicker
                    label='To Date'
                    value={reportRequest?.endDate ? new Date(reportRequest.endDate): null} 
                    onChange={(newValue:Date) => handleInputChange('toDate',newValue)}
                    renderInput={(params) => <TextField {...params} 
                    error={errorState?.toDate !== ''}
                    helperText={errorState?.toDate}
                    size='small'
                    onFocus={closeAlert}
                    fullWidth />}
                  />
                </LocalizationProvider>
